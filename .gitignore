# --- Binaries & OS-Specific ---
# Compiled Object files, Static Libraries, Shared Objects
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# Folders often used for binary output
# (If you build to a specific folder, e.g., 'go build -o bin/main')
/bin/
/dist/
/out/
/build/

# --- Test & Coverage ---
# Profiles generated by 'go test -cover'
*.out
*.test
coverage.txt
coverage.html

# --- Go Modules & Workspaces ---
# Local workspace file (Go 1.18+) - specifically for local overrides
go.work
go.work.sum

# Vendor directory
# Remove this rule if you want to commit dependencies (e.g. for hermetic builds)
vendor/

# --- Environment & Secrets ---
# NEVER commit .env files containing API keys or database credentials
.env
.env.local
*.env

# --- IDE & Editor Specific ---
# JetBrains (GoLand, IntelliJ)
.idea/
*.iws
*.iml
*.ipr

# VS Code
.vscode/
# Note: You might want to allow .vscode/tasks.json or launch.json
# but ignore .vscode/settings.json if it contains local paths.

# macOS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# Git Worktrees for AI Concurrency Workflow
worktrees/
